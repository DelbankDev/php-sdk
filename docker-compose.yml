version: '3.8'

services:
  app:
    build: .
    container_name: php-sdk-app
    volumes:
      - .:/var/www/html
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    tty: true
    stdin_open: true
